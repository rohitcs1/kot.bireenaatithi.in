# Kitchen Stations - System Architecture

## 📊 Complete System Flow

```
┌─────────────────────────────────────────────────────────────┐
│                     USER INTERFACE                          │
│                   Settings Page                             │
│                  "Stations" Tab                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │ Add Station  │  │ Edit Station │  │Delete Station│    │
│  │   Button     │  │   Button     │  │   Button     │    │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘    │
│         │                 │                 │             │
├─────────┼─────────────────┼─────────────────┼─────────────┤
│         │   FRONTEND      │                 │             │
│         │  (React)        │                 │             │
│  ┌──────▼──────┐  ┌──────▼──────┐  ┌──────▼──────┐      │
│  │POST Request │  │PUT Request  │  │DELETE Request│     │
│  │/api/stations│  │/api/stations│  │/api/stations│     │
│  │ {:id}/      │  │ {:id}       │  │ {:id}       │     │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘      │
│         │                │                │             │
├─────────┼────────────────┼────────────────┼─────────────┤
│         │   BACKEND      │                │             │
│         │  (Express)     │                │             │
│  ┌──────▼──────────────────────────────────▼──────┐    │
│  │         Middleware Chain                       │    │
│  │  authenticate → allowRoles → checkSubscription│    │
│  └──────┬──────────────────────────────────┬──────┘    │
│         │                                  │           │
│  ┌──────▼──────────────────────────────────▼──────┐    │
│  │         stations.controller.js                 │    │
│  │  ┌─────────┐  ┌────────┐  ┌──────────┐       │    │
│  │  │ create  │  │ update │  │  delete  │       │    │
│  │  │Station  │  │Station │  │Station   │       │    │
│  │  └────┬────┘  └────┬───┘  └────┬─────┘       │    │
│  │       │            │           │             │    │
│  └───────┼────────────┼───────────┼─────────────┘    │
│          │            │           │                  │
├──────────┼────────────┼───────────┼──────────────────┤
│          │  DATABASE  │           │                  │
│  ┌───────▼────────────▼───────────▼────────┐        │
│  │   Supabase PostgreSQL                   │        │
│  │   kitchen_stations table                │        │
│  │                                         │        │
│  │   ┌─────────────────────────────────┐  │        │
│  │   │ id | hotel_id | name | printer_id    │        │
│  │   │ ─────────────────────────────── │        │
│  │   │ 1  │ 5       │Main │ KOT-1      │        │
│  │   │ 2  │ 5       │Bar  │ BAR-1      │        │
│  │   │ 3  │ 5       │Des  │ KOT-1      │        │
│  │   └─────────────────────────────────┘        │
│  └─────────────────────────────────────────────┘        │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow - Create Station

```
┌──────────────────────────────────────────────────┐
│ User enters:                                     │
│ - Name: "Main Kitchen"                          │
│ - Type: "Kitchen"                               │
│ - Printer: "KOT-1"                              │
└─────────────┬──────────────────────────────────┘
              │
              ▼
┌──────────────────────────────────────────────────┐
│ Frontend Validation                              │
│ ✓ Name not empty                                │
│ ✓ Form values collected                         │
└─────────────┬──────────────────────────────────┘
              │
              ▼
┌──────────────────────────────────────────────────┐
│ POST /api/stations                              │
│ {                                              │
│   name: "Main Kitchen",                        │
│   station_type: "Kitchen",                     │
│   printer_id: "KOT-1"                          │
│ }                                              │
└─────────────┬──────────────────────────────────┘
              │
              ▼
┌──────────────────────────────────────────────────┐
│ Backend - stations.controller.createStation()   │
│ - Verify JWT token                             │
│ - Extract hotel_id from user                   │
│ - Validate name is not empty                   │
│ - Insert into kitchen_stations table           │
└─────────────┬──────────────────────────────────┘
              │
              ▼
┌──────────────────────────────────────────────────┐
│ Database - INSERT kitchen_stations              │
│ INSERT INTO kitchen_stations (                 │
│   hotel_id, name, station_type,               │
│   printer_id, enabled, created_at             │
│ ) VALUES (5, 'Main Kitchen', 'Kitchen',       │
│   'KOT-1', true, now())                       │
└─────────────┬──────────────────────────────────┘
              │
              ▼
┌──────────────────────────────────────────────────┐
│ Response with created station                  │
│ {                                              │
│   station: {                                   │
│     id: 1,                                     │
│     hotel_id: 5,                               │
│     name: "Main Kitchen",                      │
│     station_type: "Kitchen",                   │
│     printer_id: "KOT-1",                       │
│     enabled: true,                             │
│     created_at: "2024-01-01T10:00:00Z"        │
│   }                                            │
│ }                                              │
└─────────────┬──────────────────────────────────┘
              │
              ▼
┌──────────────────────────────────────────────────┐
│ Frontend Updates                                │
│ - Add station to array                         │
│ - Close modal                                  │
│ - Show success toast                           │
│ - Display new card in list                     │
└──────────────────────────────────────────────────┘
```

---

## 🔐 Security Layers

```
┌──────────────────────────────────────────┐
│         User Request                     │
│  POST /api/stations with JWT Token       │
└────────────┬─────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────┐
│  Layer 1: Authentication Middleware      │
│  ✓ Verify JWT Token Valid                │
│  ✓ Extract user info from token          │
│  ✓ Attach user to request                │
└────────────┬─────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────┐
│  Layer 2: Role Authorization Middleware  │
│  ✓ Check user.role = 'admin' or 'manager'│
│  ✓ Deny if insufficient permissions      │
│  ✓ Allow if role matches                 │
└────────────┬─────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────┐
│  Layer 3: Subscription Middleware        │
│  ✓ Check hotel has active subscription   │
│  ✓ Verify subscription not expired       │
│  ✓ Deny access if not subscribed         │
└────────────┬─────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────┐
│  Layer 4: Controller Validation          │
│  ✓ Validate input fields                 │
│  ✓ Check hotel_id matches user's hotel   │
│  ✓ Prevent cross-hotel access            │
└────────────┬─────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────┐
│  Layer 5: Database Constraints           │
│  ✓ UNIQUE(hotel_id, name) constraint     │
│  ✓ Foreign key validation                │
│  ✓ Type casting and validation           │
└────────────┬─────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────┐
│         Authorized & Validated           │
│  ✅ Request Processed Safely             │
└──────────────────────────────────────────┘
```

---

## 📱 Component Structure

```
Settings.jsx
│
├─ useEffect (on mount)
│  └─ Fetch /api/stations
│
├─ useState (stations)
│  └─ Array of station objects
│
├─ Handlers
│  ├─ handleOpenStationModal()
│  ├─ handleSaveStation() [CREATE/UPDATE]
│  ├─ handleToggleStation() [TOGGLE ENABLED]
│  └─ handleDeleteStation() [DELETE]
│
├─ Stations Tab Content
│  ├─ Station List
│  │  └─ Station Card (map)
│  │     ├─ Station Info
│  │     ├─ Toggle Checkbox
│  │     ├─ Edit Button
│  │     └─ Delete Button
│  │
│  └─ Add Station Button
│
└─ Station Modal
   ├─ Form Fields
   │  ├─ Name Input
   │  ├─ Station Type Input
   │  └─ Printer ID Input
   │
   └─ Actions
      ├─ Cancel Button
      └─ Create/Update Button
```

---

## 🌊 Request/Response Cycle

### Example: Create Station

**REQUEST:**
```http
POST /api/stations HTTP/1.1
Host: localhost:4000
Authorization: Bearer eyJhbGc...
Content-Type: application/json

{
  "name": "Main",
  "station_type": "Kitchen",
  "printer_id": "KOT-1"
}
```

**BACKEND PROCESSING:**
```
1. Verify JWT token valid ✓
2. Extract user.hotel_id = 5 ✓
3. Check user.role in ['admin', 'manager'] ✓
4. Validate name not empty ✓
5. Insert to database ✓
6. Return created object
```

**RESPONSE:**
```http
HTTP/1.1 201 Created
Content-Type: application/json

{
  "station": {
    "id": 42,
    "hotel_id": 5,
    "name": "Main",
    "station_type": "Kitchen",
    "printer_id": "KOT-1",
    "enabled": true,
    "created_at": "2024-01-15T10:30:00Z",
    "updated_at": "2024-01-15T10:30:00Z"
  }
}
```

**FRONTEND RESPONSE:**
```
1. Parse JSON response ✓
2. Add to stations array ✓
3. Close modal ✓
4. Show success toast ✓
5. Re-render station card ✓
```

---

## 🔌 API Endpoints Summary

```
GET    /api/stations                    [List]
POST   /api/stations                    [Create]
PUT    /api/stations/:id                [Update]
PATCH  /api/stations/:id/toggle         [Toggle Status]
DELETE /api/stations/:id                [Delete]

All endpoints:
- Require JWT authentication
- Check subscription status
- Write operations require admin/manager role
- Filter results by hotel_id
```

---

## 🎯 State Management

```
┌─────────────────────────────────────────┐
│ Component State (Settings.jsx)          │
├─────────────────────────────────────────┤
│ stations: Array<Station>                │
│   ├─ id: number                         │
│   ├─ hotel_id: number                   │
│   ├─ name: string                       │
│   ├─ station_type: string               │
│   ├─ printer_id: string                 │
│   ├─ enabled: boolean                   │
│   └─ created_at: ISO string             │
│                                         │
│ stationsLoading: boolean                │
│ stationsError: string                   │
│                                         │
│ newStation: Partial<Station>            │
│   ├─ name: string                       │
│   ├─ station_type: string               │
│   └─ printer_id: string                 │
│                                         │
│ editingStation: null | Station          │
│ showStationModal: boolean               │
└─────────────────────────────────────────┘
```

---

## ✨ Features Map

```
STATIONS FEATURE
│
├─ CREATE
│  ├─ Modal Form
│  ├─ Input Validation
│  ├─ API POST Request
│  └─ Database Insert
│
├─ READ
│  ├─ List Display
│  ├─ API GET Request
│  ├─ Loading State
│  └─ Error Handling
│
├─ UPDATE
│  ├─ Edit Modal
│  ├─ Form Population
│  ├─ API PUT Request
│  └─ Database Update
│
├─ DELETE
│  ├─ Confirmation Dialog
│  ├─ API DELETE Request
│  ├─ Database Delete
│  └─ UI Update
│
└─ TOGGLE
   ├─ Checkbox Component
   ├─ API PATCH Request
   ├─ Status Update
   └─ Instant Feedback
```

---

This architecture provides a secure, scalable, and maintainable system for managing kitchen stations in the restaurant management application.
